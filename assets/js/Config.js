const CONFIG = {
    SERVER: 'http://localhost:3000', // Without / in the end
    SERVER_SIGNIN: '/auth/signin', // Signin endpoint
    SERVER_AUTHENDPOINT: '/auth-endpoint' // Authentication endpoint
}

export default CONFIG
